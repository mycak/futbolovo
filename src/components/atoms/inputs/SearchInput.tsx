{
  /* <span className="text-grass-20 mb-1">{label}</span>
      <label>
        <select
          name={name}
          id={name}
          className="border border-grass-40 bg-emerald-900 rounded-md p-2 outline-none"
          multiple={multiple}
          value=""
          onChange={(e) => console.log(e.target.value)}
        >
          {options.map((option) => (
            <option key={option.value} value={option.value}>
              {option.text}
            </option>
          ))}
          <option value="" disabled selected>
            {placeholder}
          </option>
        </select>
      </label> */
}
